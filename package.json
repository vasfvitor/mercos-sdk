{
  "name": "mercos-sdk",
  "version": "0.1.2",
  "description": "Unofficial TypeScript SDK for the Mercos integration API",
  "license": "MIT",
  "author": "vasfvitor (https://github.com/vasfvitor)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vasfvitor/mercos-sdk.git"
  },
  "homepage": "https://github.com/vasfvitor/mercos-sdk#readme",
  "bugs": "https://github.com/vasfvitor/mercos-sdk/issues",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "!dist/generated/*.map",
    "README.md",
    "README.en.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "keywords": [
    "mercos",
    "meuspedidos",
    "sdk",
    "api",
    "pedidos",
    "b2b"
  ],
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "test:live": "node --test --test-concurrency=1 \"test/live/*.test.ts\"",
    "test:coverage": "node --test --experimental-test-coverage \"test/*.test.ts\"",
    "check": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "verify": "pnpm lint && pnpm check && pnpm spec:lint && pnpm test",
    "spec:fetch": "node scripts/fetch-spec.ts",
    "spec:merge": "node scripts/merge-spec.ts",
    "spec:types": "node scripts/gen-types.ts",
    "spec:fixtures": "node scripts/extract-fixtures.ts",
    "spec:lint": "node scripts/lint-spec.ts",
    "spec:check": "node scripts/check-spec.ts",
    "spec": "pnpm spec:fetch && pnpm spec:merge && pnpm spec:types && pnpm spec:fixtures && pnpm spec:check --update",
    "prepublishOnly": "pnpm verify && pnpm build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2",
    "@redocly/openapi-core": "^1.34.20",
    "@types/node": "^24",
    "openapi-typescript": "^7",
    "typescript": "^5.9"
  }
}
